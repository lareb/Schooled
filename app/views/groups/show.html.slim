= render 'requests'

table.table-responsive.col-xs-12
  thead
    tr
      th.col-xs-6 = t 'table_header.name' 
      th.col-xs-5 = t 'table_header.actions' 

  tbody
    - @group.students.each do |student|
      tr
        td.col-xs-2 = link_to student.name, user_path(student)
        td.col-xs-5 

